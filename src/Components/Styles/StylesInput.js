import {StyleSheet} from 'react-native';

const stylesInput = StyleSheet.create({
    textInput: {
        borderWidth: 1,
        borderColor: 'black',
        borderRadius: 10,
        width: '80%',
        padding: 10,
        paddingLeft: 15,
        marginBottom: 10,
    },
})

export default stylesInput;